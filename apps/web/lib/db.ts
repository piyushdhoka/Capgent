/**
 * Harmless placeholder for db.ts to prevent build errors.
 * Prisma and Better Auth are temporarily disabled.
 */
export const db = {} as any;
export const PrismaClient = class {} as any;
export const PrismaNeon = class {} as any;
