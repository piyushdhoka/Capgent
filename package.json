{
  "name": "capgent",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun run --cwd apps/api dev",
    "dev:web": "bun run --cwd apps/web dev",
    "build:sdk": "bun run --cwd packages/sdk build",
    "build:api": "bun run --cwd apps/api build",
    "build:web": "bun run --cwd apps/web build",
    "build": "bun run build:sdk; bun run build:api; bun run build:web",
    "typecheck": "bun run --cwd apps/api typecheck; bun run --cwd apps/web typecheck; bun run --cwd packages/sdk typecheck"
  }
}
